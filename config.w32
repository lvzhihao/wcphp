// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("wordcheck", "for wordcheck support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("wordcheck", "enable wordcheck support", "no");

if (PHP_WORDCHECK != "no") {
	EXTENSION("wordcheck", "wordcheck.c");
}

